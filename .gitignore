# Downloaded binary assets (150MB) — re-fetchable from extraction/assets/manifest.json
extraction/assets/files/
# Raw HTML/JSON crawl dumps — normalized versions kept in extraction/content/
extraction/raw/

# Node / Next.js (Phase 4)
node_modules/
.next/
out/
.env
.env.local
npm-debug.log*
.DS_Store
design-analysis/raw/

# QA screenshots (regenerable evidence)
qa/shots-iter*/
qa/*.jpg
qa/video/

# Vendored design-intelligence skill (re-installable via npx ui-ux-pro-max-cli)
.claude/skills/
